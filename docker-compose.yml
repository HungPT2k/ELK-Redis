version: "3"

services:

    elasticsearch:
      image: docker.elastic.co/elasticsearch/elasticsearch:8.3.3
      container_name: elasticsearch_springboot
      environment:
          - bootstrap.memory_lock=true
          - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
          - "discovery.type=single-node"
          - xpack.security.enabled=false
      ports:
          - "9200:9200"
      volumes:
          - elasticsearch_data:/usr/share/elasticsearch/data
      networks:
          - elastic

    kibana:
      image: docker.elastic.co/kibana/kibana:8.3.3
      container_name: kibana_springboot
      ports:
          - "5601:5601"
      environment:
          ELASTICSEARCH_URL: http://elasticsearch:9200
          ELASTICSEARCH_HOSTS: '["http://elasticsearch:9200"]'
      depends_on:
          - elasticsearch
      networks:
          - elastic

    logstash:
      image: docker.elastic.co/logstash/logstash:8.3.3
      container_name: logstash_springboot
      volumes:
        - ./logstash/config/logstash.yml:/etc/logstash/conf.d/logstash.yml:ro
        - ./logstash/pipeline:/usr/share/logstash/pipeline:ro
        - ./logs/application.log:/usr/share/logstash/application.log:ro
        - ./db/postgresql-42.6.0.jar:/usr/share/logstash/config/postgresql-42.6.0.jar:ro
      ports:
        - "5044:5044"
        - "5000:5000/tcp"
        - "5000:5000/udp"
        - "9600:9600"
      environment:
        LS_JAVA_OPTS: "-Xmx256m -Xms256m"
      networks:
        - elastic
      depends_on:
        - elasticsearch
#    db:
#      image: mysql:8.0
#      command: --default-authentication-plugin=caching_sha2_password
#      restart: always
#      environment:
#        MYSQL_HOST: localhost
#        MYSQL_ROOT_PASSWORD: 123
#        MYSQL_DATABASE: ecom5
#        MYSQL_USER: root
#        MYSQL_PASSWORD: 123
#      volumes:
#        - ./db/mysql-connector-java-8.0.28.jar:/usr/share/file/mysql-connector-java-8.0.28.jar:ro
#      ports:
#        - "3309:3306"
#      networks:
#        - elastic
    postgres:
        image: postgres:14.1-alpine
        container_name: postgres_springboot
        restart: always
        environment:
          - POSTGRES_USER=postgres
          - POSTGRES_PASSWORD=123
        ports:
          - '5433:5432'
        networks:
          - elastic
networks:
  elastic:
    driver: bridge

volumes:
    elasticsearch_data:
        driver: local